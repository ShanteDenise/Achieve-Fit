'use strict';

const ChangeStream = require('./change_stream');
const Db = require('./db');
const EventEmitter = require('events').EventEmitter;
const executeOperation = require('./utils').executeOperation;
const handleCallback = require('./utils').handleCallback;
const inherits = require('util').inherits;
const MongoError = require('mongodb-core').MongoError;

// Operations
const connectOp = require('./operations/mongo_client_ops').connectOp;
const logout = require('./operations/mongo_client_ops').logout;
const validOptions = require('./operations/mongo_client_ops').validOptions;

/**
 * @fileOverview The **MongoClient** class is a class that allows for making Connections to MongoDB.
 *
 * @example
 * // Connect using a MongoClient instance
 * const MongoClient = require('mongodb').MongoClient;
 * const test = require('assert');
 * // Connection url
 * const url = 'mongodb://localhost:27017';
 * // Database Name
 * const dbName = 'test';
 * // Connect using MongoClient
 * const mongoClient = new MongoClient(url);
 * mongoClient.connect(function(err, client) {
 *   const db = client.db(dbName);
 *   client.close();
 * ***REMOVED***);
 *
 * @example
 * // Connect using the MongoClient.connect static method
 * const MongoClient = require('mongodb').MongoClient;
 * const test = require('assert');
 * // Connection url
 * const url = 'mongodb://localhost:27017';
 * // Database Name
 * const dbName = 'test';
 * // Connect using MongoClient
 * MongoClient.connect(url, function(err, client) {
 *   const db = client.db(dbName);
 *   client.close();
 * ***REMOVED***);
 */

/**
 * Creates a new MongoClient instance
 * @class
 * @param {string***REMOVED*** url The connection URI string
 * @param {object***REMOVED*** [options] Optional settings
 * @param {number***REMOVED*** [options.poolSize=5] The maximum size of the individual server pool
 * @param {boolean***REMOVED*** [options.ssl=false] Enable SSL connection.
 * @param {boolean***REMOVED*** [options.sslValidate=true] Validate mongod server certificate against Certificate Authority
 * @param {buffer***REMOVED*** [options.sslCA=undefined] SSL Certificate store binary buffer
 * @param {buffer***REMOVED*** [options.sslCert=undefined] SSL Certificate binary buffer
 * @param {buffer***REMOVED*** [options.sslKey=undefined] SSL Key file binary buffer
 * @param {string***REMOVED*** [options.sslPass=undefined] SSL Certificate pass phrase
 * @param {buffer***REMOVED*** [options.sslCRL=undefined] SSL Certificate revocation list binary buffer
 * @param {boolean***REMOVED*** [options.autoReconnect=true] Enable autoReconnect for single server instances
 * @param {boolean***REMOVED*** [options.noDelay=true] TCP Connection no delay
 * @param {boolean***REMOVED*** [options.keepAlive=true] TCP Connection keep alive enabled
 * @param {number***REMOVED*** [options.keepAliveInitialDelay=30000] The number of milliseconds to wait before initiating keepAlive on the TCP socket
 * @param {number***REMOVED*** [options.connectTimeoutMS=30000] TCP Connection timeout setting
 * @param {number***REMOVED*** [options.family] Version of IP stack. Can be 4, 6 or null (default).
 * If null, will attempt to connect with IPv6, and will fall back to IPv4 on failure
 * @param {number***REMOVED*** [options.socketTimeoutMS=360000] TCP Socket timeout setting
 * @param {number***REMOVED*** [options.reconnectTries=30] Server attempt to reconnect #times
 * @param {number***REMOVED*** [options.reconnectInterval=1000] Server will wait # milliseconds between retries
 * @param {boolean***REMOVED*** [options.ha=true] Control if high availability monitoring runs for Replicaset or Mongos proxies
 * @param {number***REMOVED*** [options.haInterval=10000] The High availability period for replicaset inquiry
 * @param {string***REMOVED*** [options.replicaSet=undefined] The Replicaset set name
 * @param {number***REMOVED*** [options.secondaryAcceptableLatencyMS=15] Cutoff latency point in MS for Replicaset member selection
 * @param {number***REMOVED*** [options.acceptableLatencyMS=15] Cutoff latency point in MS for Mongos proxies selection
 * @param {boolean***REMOVED*** [options.connectWithNoPrimary=false] Sets if the driver should connect even if no primary is available
 * @param {string***REMOVED*** [options.authSource=undefined] Define the database to authenticate against
 * @param {(number|string)***REMOVED*** [options.w] The write concern
 * @param {number***REMOVED*** [options.wtimeout] The write concern timeout
 * @param {boolean***REMOVED*** [options.j=false] Specify a journal write concern
 * @param {boolean***REMOVED*** [options.forceServerObjectId=false] Force server to assign _id values instead of driver
 * @param {boolean***REMOVED*** [options.serializeFunctions=false] Serialize functions on any object
 * @param {Boolean***REMOVED*** [options.ignoreUndefined=false] Specify if the BSON serializer should ignore undefined fields
 * @param {boolean***REMOVED*** [options.raw=false] Return document results as raw BSON buffers
 * @param {number***REMOVED*** [options.bufferMaxEntries=-1] Sets a cap on how many operations the driver will buffer up before giving up on getting a working connection, default is -1 which is unlimited
 * @param {(ReadPreference|string)***REMOVED*** [options.readPreference] The preferred read preference (ReadPreference.PRIMARY, ReadPreference.PRIMARY_PREFERRED, ReadPreference.SECONDARY, ReadPreference.SECONDARY_PREFERRED, ReadPreference.NEAREST)
 * @param {object***REMOVED*** [options.pkFactory] A primary key factory object for generation of custom _id keys
 * @param {object***REMOVED*** [options.promiseLibrary] A Promise library class the application wishes to use such as Bluebird, must be ES6 compatible
 * @param {object***REMOVED*** [options.readConcern] Specify a read concern for the collection (only MongoDB 3.2 or higher supported)
 * @param {string***REMOVED*** [options.readConcern.level='local'] Specify a read concern level for the collection operations, one of [local|majority]. (only MongoDB 3.2 or higher supported)
 * @param {number***REMOVED*** [options.maxStalenessSeconds=undefined] The max staleness to secondary reads (values under 10 seconds cannot be guaranteed)
 * @param {string***REMOVED*** [options.loggerLevel=undefined] The logging level (error/warn/info/debug)
 * @param {object***REMOVED*** [options.logger=undefined] Custom logger object
 * @param {boolean***REMOVED*** [options.promoteValues=true] Promotes BSON values to native types where possible, set to false to only receive wrapper types
 * @param {boolean***REMOVED*** [options.promoteBuffers=false] Promotes Binary BSON values to native Node Buffers
 * @param {boolean***REMOVED*** [options.promoteLongs=true] Promotes long values to number if they fit inside the 53 bits resolution
 * @param {boolean***REMOVED*** [options.domainsEnabled=false] Enable the wrapping of the callback in the current domain, disabled by default to avoid perf hit
 * @param {boolean|function***REMOVED*** [options.checkServerIdentity=true] Ensure we check server identify during SSL, set to false to disable checking. Only works for Node 0.12.x or higher. You can pass in a boolean or your own checkServerIdentity override function
 * @param {object***REMOVED*** [options.validateOptions=false] Validate MongoClient passed in options for correctness
 * @param {string***REMOVED*** [options.appname=undefined] The name of the application that created this MongoClient instance. MongoDB 3.4 and newer will print this value in the server log upon establishing each connection. It is also recorded in the slow query log and profile collections
 * @param {string***REMOVED*** [options.auth.user=undefined] The username for auth
 * @param {string***REMOVED*** [options.auth.password=undefined] The password for auth
 * @param {string***REMOVED*** [options.authMechanism=undefined] Mechanism for authentication: MDEFAULT, GSSAPI, PLAIN, MONGODB-X509, or SCRAM-SHA-1
 * @param {object***REMOVED*** [options.compression] Type of compression to use: snappy or zlib
 * @param {boolean***REMOVED*** [options.fsync=false] Specify a file sync write concern
 * @param {array***REMOVED*** [options.readPreferenceTags] Read preference tags
 * @param {number***REMOVED*** [options.numberOfRetries=5] The number of retries for a tailable cursor
 * @param {boolean***REMOVED*** [options.auto_reconnect=true] Enable auto reconnecting for single server instances
 * @param {boolean***REMOVED*** [options.monitorCommands=false] Enable command monitoring for this client
 * @param {number***REMOVED*** [options.minSize] If present, the connection pool will be initialized with minSize connections, and will never dip below minSize connections
 * @param {boolean***REMOVED*** [options.useNewUrlParser=false] Determines whether or not to use the new url parser
 * @param {MongoClient~connectCallback***REMOVED*** [callback] The command result callback
 * @return {MongoClient***REMOVED*** a MongoClient instance
 */
function MongoClient(url, options) {
  if (!(this instanceof MongoClient)) return new MongoClient(url, options);
  // Set up event emitter
  EventEmitter.call(this);

  // The internal state
  this.s = {
    url: url,
    options: options || {***REMOVED***,
    promiseLibrary: null,
    dbCache: {***REMOVED***,
    sessions: []
  ***REMOVED***;

  // Get the promiseLibrary
  const promiseLibrary = this.s.options.promiseLibrary || Promise;

  // Add the promise to the internal state
  this.s.promiseLibrary = promiseLibrary;
***REMOVED***

/**
 * @ignore
 */
inherits(MongoClient, EventEmitter);

/**
 * The callback format for results
 * @callback MongoClient~connectCallback
 * @param {MongoError***REMOVED*** error An error instance representing the error during the execution.
 * @param {MongoClient***REMOVED*** client The connected client.
 */

/**
 * Connect to MongoDB using a url as documented at
 *
 *  docs.mongodb.org/manual/reference/connection-string/
 *
 * Note that for replicasets the replicaSet query parameter is required in the 2.0 driver
 *
 * @method
 * @param {MongoClient~connectCallback***REMOVED*** [callback] The command result callback
 * @return {Promise<MongoClient>***REMOVED*** returns Promise if no callback passed
 */
MongoClient.prototype.connect = function(callback) {
  // Validate options object
  const err = validOptions(this.s.options);

  if (typeof callback === 'string') {
    throw new TypeError('`connect` only accepts a callback');
  ***REMOVED***

  return executeOperation(this, connectOp, [this, err, callback], {
    skipSessions: true
  ***REMOVED***);
***REMOVED***;

/**
 * Logout user from server, fire off on all connections and remove all auth info
 * @method
 * @param {object***REMOVED*** [options] Optional settings.
 * @param {string***REMOVED*** [options.dbName] Logout against different database than current.
 * @param {Db~resultCallback***REMOVED*** [callback] The command result callback
 * @return {Promise***REMOVED*** returns Promise if no callback passed
 */
MongoClient.prototype.logout = function(options, callback) {
  if (typeof options === 'function') (callback = options), (options = {***REMOVED***);
  options = options || {***REMOVED***;

  // Establish the correct database name
  const dbName = this.s.options.authSource ? this.s.options.authSource : this.s.options.dbName;

  return executeOperation(this, logout, [this, dbName, callback], {
    skipSessions: true
  ***REMOVED***);
***REMOVED***;

/**
 * Close the db and its underlying connections
 * @method
 * @param {boolean***REMOVED*** force Force close, emitting no events
 * @param {Db~noResultCallback***REMOVED*** [callback] The result callback
 * @return {Promise***REMOVED*** returns Promise if no callback passed
 */
MongoClient.prototype.close = function(force, callback) {
  if (typeof force === 'function') (callback = force), (force = false);

  // Close the topology connection
  if (this.topology) {
    this.topology.close(force);
  ***REMOVED***
  // Emit close event
  this.emit('close', this);

  // Fire close event on any cached db instances
  for (const name in this.s.dbCache) {
    this.s.dbCache[name].emit('close');
  ***REMOVED***

  // Remove listeners after emit
  this.removeAllListeners('close');

  // Callback after next event loop tick
  if (typeof callback === 'function')
    return process.nextTick(() => {
      handleCallback(callback, null);
    ***REMOVED***);

  // Return dummy promise
  return new this.s.promiseLibrary(resolve => {
    resolve();
  ***REMOVED***);
***REMOVED***;

/**
 * Create a new Db instance sharing the current socket connections. Be aware that the new db instances are
 * related in a parent-child relationship to the original instance so that events are correctly emitted on child
 * db instances. Child db instances are cached so performing db('db1') twice will return the same instance.
 * You can control these behaviors with the options noListener and returnNonCachedInstance.
 *
 * @method
 * @param {string***REMOVED*** [dbName] The name of the database we want to use. If not provided, use database name from connection string.
 * @param {object***REMOVED*** [options] Optional settings.
 * @param {boolean***REMOVED*** [options.noListener=false] Do not make the db an event listener to the original connection.
 * @param {boolean***REMOVED*** [options.returnNonCachedInstance=false] Control if you want to return a cached instance or have a new one created
 * @return {Db***REMOVED***
 */
MongoClient.prototype.db = function(dbName, options) {
  options = options || {***REMOVED***;

  // Default to db from connection string if not provided
  if (!dbName) {
    dbName = this.s.options.dbName;
  ***REMOVED***

  // Copy the options and add out internal override of the not shared flag
  const finalOptions = Object.assign({***REMOVED***, this.s.options, options);

  // Do we have the db in the cache already
  if (this.s.dbCache[dbName] && finalOptions.returnNonCachedInstance !== true) {
    return this.s.dbCache[dbName];
  ***REMOVED***

  // Add promiseLibrary
  finalOptions.promiseLibrary = this.s.promiseLibrary;

  // If no topology throw an error message
  if (!this.topology) {
    throw new MongoError('MongoClient must be connected before calling MongoClient.prototype.db');
  ***REMOVED***

  // Return the db object
  const db = new Db(dbName, this.topology, finalOptions);

  // Add the db to the cache
  this.s.dbCache[dbName] = db;
  // Return the database
  return db;
***REMOVED***;

/**
 * Check if MongoClient is connected
 *
 * @method
 * @param {object***REMOVED*** [options] Optional settings.
 * @param {boolean***REMOVED*** [options.noListener=false] Do not make the db an event listener to the original connection.
 * @param {boolean***REMOVED*** [options.returnNonCachedInstance=false] Control if you want to return a cached instance or have a new one created
 * @return {boolean***REMOVED***
 */
MongoClient.prototype.isConnected = function(options) {
  options = options || {***REMOVED***;

  if (!this.topology) return false;
  return this.topology.isConnected(options);
***REMOVED***;

/**
 * Connect to MongoDB using a url as documented at
 *
 *  docs.mongodb.org/manual/reference/connection-string/
 *
 * Note that for replicasets the replicaSet query parameter is required in the 2.0 driver
 *
 * @method
 * @static
 * @param {string***REMOVED*** url The connection URI string
 * @param {object***REMOVED*** [options] Optional settings
 * @param {number***REMOVED*** [options.poolSize=5] The maximum size of the individual server pool
 * @param {boolean***REMOVED*** [options.ssl=false] Enable SSL connection.
 * @param {boolean***REMOVED*** [options.sslValidate=true] Validate mongod server certificate against Certificate Authority
 * @param {buffer***REMOVED*** [options.sslCA=undefined] SSL Certificate store binary buffer
 * @param {buffer***REMOVED*** [options.sslCert=undefined] SSL Certificate binary buffer
 * @param {buffer***REMOVED*** [options.sslKey=undefined] SSL Key file binary buffer
 * @param {string***REMOVED*** [options.sslPass=undefined] SSL Certificate pass phrase
 * @param {buffer***REMOVED*** [options.sslCRL=undefined] SSL Certificate revocation list binary buffer
 * @param {boolean***REMOVED*** [options.autoReconnect=true] Enable autoReconnect for single server instances
 * @param {boolean***REMOVED*** [options.noDelay=true] TCP Connection no delay
 * @param {boolean***REMOVED*** [options.keepAlive=true] TCP Connection keep alive enabled
 * @param {boolean***REMOVED*** [options.keepAliveInitialDelay=30000] The number of milliseconds to wait before initiating keepAlive on the TCP socket
 * @param {number***REMOVED*** [options.connectTimeoutMS=30000] TCP Connection timeout setting
 * @param {number***REMOVED*** [options.family] Version of IP stack. Can be 4, 6 or null (default).
 * If null, will attempt to connect with IPv6, and will fall back to IPv4 on failure
 * @param {number***REMOVED*** [options.socketTimeoutMS=360000] TCP Socket timeout setting
 * @param {number***REMOVED*** [options.reconnectTries=30] Server attempt to reconnect #times
 * @param {number***REMOVED*** [options.reconnectInterval=1000] Server will wait # milliseconds between retries
 * @param {boolean***REMOVED*** [options.ha=true] Control if high availability monitoring runs for Replicaset or Mongos proxies
 * @param {number***REMOVED*** [options.haInterval=10000] The High availability period for replicaset inquiry
 * @param {string***REMOVED*** [options.replicaSet=undefined] The Replicaset set name
 * @param {number***REMOVED*** [options.secondaryAcceptableLatencyMS=15] Cutoff latency point in MS for Replicaset member selection
 * @param {number***REMOVED*** [options.acceptableLatencyMS=15] Cutoff latency point in MS for Mongos proxies selection
 * @param {boolean***REMOVED*** [options.connectWithNoPrimary=false] Sets if the driver should connect even if no primary is available
 * @param {string***REMOVED*** [options.authSource=undefined] Define the database to authenticate against
 * @param {(number|string)***REMOVED*** [options.w] The write concern
 * @param {number***REMOVED*** [options.wtimeout] The write concern timeout
 * @param {boolean***REMOVED*** [options.j=false] Specify a journal write concern
 * @param {boolean***REMOVED*** [options.forceServerObjectId=false] Force server to assign _id values instead of driver
 * @param {boolean***REMOVED*** [options.serializeFunctions=false] Serialize functions on any object
 * @param {Boolean***REMOVED*** [options.ignoreUndefined=false] Specify if the BSON serializer should ignore undefined fields
 * @param {boolean***REMOVED*** [options.raw=false] Return document results as raw BSON buffers
 * @param {number***REMOVED*** [options.bufferMaxEntries=-1] Sets a cap on how many operations the driver will buffer up before giving up on getting a working connection, default is -1 which is unlimited
 * @param {(ReadPreference|string)***REMOVED*** [options.readPreference] The preferred read preference (ReadPreference.PRIMARY, ReadPreference.PRIMARY_PREFERRED, ReadPreference.SECONDARY, ReadPreference.SECONDARY_PREFERRED, ReadPreference.NEAREST)
 * @param {object***REMOVED*** [options.pkFactory] A primary key factory object for generation of custom _id keys
 * @param {object***REMOVED*** [options.promiseLibrary] A Promise library class the application wishes to use such as Bluebird, must be ES6 compatible
 * @param {object***REMOVED*** [options.readConcern] Specify a read concern for the collection (only MongoDB 3.2 or higher supported)
 * @param {string***REMOVED*** [options.readConcern.level='local'] Specify a read concern level for the collection operations, one of [local|majority]. (only MongoDB 3.2 or higher supported)
 * @param {number***REMOVED*** [options.maxStalenessSeconds=undefined] The max staleness to secondary reads (values under 10 seconds cannot be guaranteed)
 * @param {string***REMOVED*** [options.loggerLevel=undefined] The logging level (error/warn/info/debug)
 * @param {object***REMOVED*** [options.logger=undefined] Custom logger object
 * @param {boolean***REMOVED*** [options.promoteValues=true] Promotes BSON values to native types where possible, set to false to only receive wrapper types
 * @param {boolean***REMOVED*** [options.promoteBuffers=false] Promotes Binary BSON values to native Node Buffers
 * @param {boolean***REMOVED*** [options.promoteLongs=true] Promotes long values to number if they fit inside the 53 bits resolution
 * @param {boolean***REMOVED*** [options.domainsEnabled=false] Enable the wrapping of the callback in the current domain, disabled by default to avoid perf hit
 * @param {boolean|function***REMOVED*** [options.checkServerIdentity=true] Ensure we check server identify during SSL, set to false to disable checking. Only works for Node 0.12.x or higher. You can pass in a boolean or your own checkServerIdentity override function
 * @param {object***REMOVED*** [options.validateOptions=false] Validate MongoClient passed in options for correctness
 * @param {string***REMOVED*** [options.appname=undefined] The name of the application that created this MongoClient instance. MongoDB 3.4 and newer will print this value in the server log upon establishing each connection. It is also recorded in the slow query log and profile collections
 * @param {string***REMOVED*** [options.auth.user=undefined] The username for auth
 * @param {string***REMOVED*** [options.auth.password=undefined] The password for auth
 * @param {string***REMOVED*** [options.authMechanism=undefined] Mechanism for authentication: MDEFAULT, GSSAPI, PLAIN, MONGODB-X509, or SCRAM-SHA-1
 * @param {object***REMOVED*** [options.compression] Type of compression to use: snappy or zlib
 * @param {boolean***REMOVED*** [options.fsync=false] Specify a file sync write concern
 * @param {array***REMOVED*** [options.readPreferenceTags] Read preference tags
 * @param {number***REMOVED*** [options.numberOfRetries=5] The number of retries for a tailable cursor
 * @param {boolean***REMOVED*** [options.auto_reconnect=true] Enable auto reconnecting for single server instances
 * @param {number***REMOVED*** [options.minSize] If present, the connection pool will be initialized with minSize connections, and will never dip below minSize connections
 * @param {MongoClient~connectCallback***REMOVED*** [callback] The command result callback
 * @return {Promise<MongoClient>***REMOVED*** returns Promise if no callback passed
 */
MongoClient.connect = function(url, options, callback) {
  const args = Array.prototype.slice.call(arguments, 1);
  callback = typeof args[args.length - 1] === 'function' ? args.pop() : undefined;
  options = args.length ? args.shift() : null;
  options = options || {***REMOVED***;

  // Create client
  const mongoClient = new MongoClient(url, options);
  // Execute the connect method
  return mongoClient.connect(callback);
***REMOVED***;

/**
 * Starts a new session on the server
 *
 * @param {SessionOptions***REMOVED*** [options] optional settings for a driver session
 * @return {ClientSession***REMOVED*** the newly established session
 */
MongoClient.prototype.startSession = function(options) {
  options = Object.assign({ explicit: true ***REMOVED***, options);
  if (!this.topology) {
    throw new MongoError('Must connect to a server before calling this method');
  ***REMOVED***

  if (!this.topology.hasSessionSupport()) {
    throw new MongoError('Current topology does not support sessions');
  ***REMOVED***

  return this.topology.startSession(options, this.s.options);
***REMOVED***;

/**
 * Runs a given operation with an implicitly created session. The lifetime of the session
 * will be handled without the need for user interaction.
 *
 * NOTE: presently the operation MUST return a Promise (either explicit or implicity as an async function)
 *
 * @param {Object***REMOVED*** [options] Optional settings to be appled to implicitly created session
 * @param {Function***REMOVED*** operation An operation to execute with an implicitly created session. The signature of this MUST be `(session) => {***REMOVED***`
 * @return {Promise***REMOVED***
 */
MongoClient.prototype.withSession = function(options, operation) {
  if (typeof options === 'function') (operation = options), (options = undefined);
  const session = this.startSession(options);

  let cleanupHandler = (err, result, opts) => {
    // prevent multiple calls to cleanupHandler
    cleanupHandler = () => {
      throw new ReferenceError('cleanupHandler was called too many times');
    ***REMOVED***;

    opts = Object.assign({ throw: true ***REMOVED***, opts);
    session.endSession();

    if (err) {
      if (opts.throw) throw err;
      return Promise.reject(err);
    ***REMOVED***
  ***REMOVED***;

  try {
    const result = operation(session);
    return Promise.resolve(result)
      .then(result => cleanupHandler(null, result))
      .catch(err => cleanupHandler(err, null, { throw: true ***REMOVED***));
  ***REMOVED*** catch (err) {
    return cleanupHandler(err, null, { throw: false ***REMOVED***);
  ***REMOVED***
***REMOVED***;
/**
 * Create a new Change Stream, watching for new changes (insertions, updates, replacements, deletions, and invalidations) in this cluster. Will ignore all changes to system collections, as well as the local, admin,
 * and config databases.
 * @method
 * @since 3.1.0
 * @param {Array***REMOVED*** [pipeline] An array of {@link https://docs.mongodb.com/manual/reference/operator/aggregation-pipeline/|aggregation pipeline stages***REMOVED*** through which to pass change stream documents. This allows for filtering (using $match) and manipulating the change stream documents.
 * @param {object***REMOVED*** [options] Optional settings
 * @param {string***REMOVED*** [options.fullDocument='default'] Allowed values: ‘default’, ‘updateLookup’. When set to ‘updateLookup’, the change stream will include both a delta describing the changes to the document, as well as a copy of the entire document that was changed from some time after the change occurred.
 * @param {object***REMOVED*** [options.resumeAfter] Specifies the logical starting point for the new change stream. This should be the _id field from a previously returned change stream document.
 * @param {number***REMOVED*** [options.maxAwaitTimeMS] The maximum amount of time for the server to wait on new documents to satisfy a change stream query
 * @param {number***REMOVED*** [options.batchSize] The number of documents to return per batch. See {@link https://docs.mongodb.com/manual/reference/command/aggregate|aggregation documentation***REMOVED***.
 * @param {object***REMOVED*** [options.collation] Specify collation settings for operation. See {@link https://docs.mongodb.com/manual/reference/command/aggregate|aggregation documentation***REMOVED***.
 * @param {ReadPreference***REMOVED*** [options.readPreference] The read preference. See {@link https://docs.mongodb.com/manual/reference/read-preference|read preference documentation***REMOVED***.
 * @param {Timestamp***REMOVED*** [options.startAtClusterTime] receive change events that occur after the specified timestamp
 * @param {ClientSession***REMOVED*** [options.session] optional session to use for this operation
 * @return {ChangeStream***REMOVED*** a ChangeStream instance.
 */
MongoClient.prototype.watch = function(pipeline, options) {
  pipeline = pipeline || [];
  options = options || {***REMOVED***;

  // Allow optionally not specifying a pipeline
  if (!Array.isArray(pipeline)) {
    options = pipeline;
    pipeline = [];
  ***REMOVED***

  return new ChangeStream(this, pipeline, options);
***REMOVED***;

/**
 * Return the mongo client logger
 * @method
 * @return {Logger***REMOVED*** return the mongo client logger
 * @ignore
 */
MongoClient.prototype.getLogger = function() {
  return this.s.options.logger;
***REMOVED***;

module.exports = MongoClient;
