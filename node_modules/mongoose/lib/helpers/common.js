'use strict';

/*!
 * Module dependencies.
 */

const ObjectId = require('../types/objectid');
const utils = require('../utils');

exports.flatten = flatten;
exports.modifiedPaths = modifiedPaths;

/*!
 * ignore
 */

function flatten(update, path, options) {
  let keys;
  if (update && utils.isMongooseObject(update) && !Buffer.isBuffer(update)) {
    keys = Object.keys(update.toObject({ transform: false, virtuals: false ***REMOVED***));
  ***REMOVED***
    keys = Object.keys(update || {***REMOVED***);
  ***REMOVED***

  const numKeys = keys.length;
  const result = {***REMOVED***;
  path = path ? path + '.' : '';

  for (let i = 0; i < numKeys; ++i) {
    const key = keys[i];
    const val = update[key];
    result[path + key] = val;
    if (shouldFlatten(val)) {
      if (options && options.skipArrays && Array.isArray(val)) {
        continue;
      ***REMOVED***
      const flat = flatten(val, path + key, options);
      for (const k in flat) {
        result[k] = flat[k];
      ***REMOVED***
      if (Array.isArray(val)) {
        result[path + key] = val;
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  return result;
***REMOVED***

/*!
 * ignore
 */

function modifiedPaths(update, path, result) {
  const keys = Object.keys(update || {***REMOVED***);
  const numKeys = keys.length;
  result = result || {***REMOVED***;
  path = path ? path + '.' : '';

  for (let i = 0; i < numKeys; ++i) {
    const key = keys[i];
    let val = update[key];

    result[path + key] = true;
    if (utils.isMongooseObject(val) && !Buffer.isBuffer(val)) {
      val = val.toObject({ transform: false, virtuals: false ***REMOVED***);
    ***REMOVED***
    if (shouldFlatten(val)) {
      modifiedPaths(val, path + key, result);
    ***REMOVED***
  ***REMOVED***

  return result;
***REMOVED***

/*!
 * ignore
 */

function shouldFlatten(val) {
  return val &&
    typeof val === 'object' &&
    !(val instanceof Date) &&
    !(val instanceof ObjectId) &&
    (!Array.isArray(val) || val.length > 0) &&
    !(val instanceof Buffer);
***REMOVED***
