'use strict';

const CastError = require('../error/cast');

/*!
 * Given a value, cast it to a number, or throw a `CastError` if the value
 * cannot be casted. `null` and `undefined` are considered valid.
 *
 * @param {Any***REMOVED*** value
 * @param {String***REMOVED*** [path] optional the path to set on the CastError
 * @return {Boolean|null|undefined***REMOVED***
 * @throws {CastError***REMOVED*** if `value` is not one of the allowed values
 * @api private
 */

module.exports = function castNumber(val, path) {
  if (isNaN(val)) {
    throw new CastError('number', val, path);
  ***REMOVED***

  if (val == null) {
    return val;
  ***REMOVED***
  if (val === '') {
    return null;
  ***REMOVED***

  if (typeof val === 'string' || typeof val === 'boolean') {
    val = Number(val);
  ***REMOVED***

  if (isNaN(val)) {
    throw new CastError('number', val, path);
  ***REMOVED***
  if (val instanceof Number) {
    return val;
  ***REMOVED***
  if (typeof val === 'number') {
    return val;
  ***REMOVED***
  if (!Array.isArray(val) && typeof val.valueOf === 'function') {
    return Number(val.valueOf());
  ***REMOVED***
  if (val.toString && !Array.isArray(val) && val.toString() == Number(val)) {
    return new Number(val);
  ***REMOVED***

  throw new CastError('number', val, path);
***REMOVED***;
