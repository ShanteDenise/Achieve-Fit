'use strict';

const each = require('async/each');

/*!
 * ignore
 */

module.exports = function(schema) {
  const unshift = true;
  schema.s.hooks.pre('remove', false, function(next) {
    if (this.ownerDocument) {
      next();
      return;
    ***REMOVED***

    const _this = this;
    const subdocs = this.$__getAllSubdocs();

    if (!subdocs.length) {
      next();
      return;
    ***REMOVED***

    each(subdocs, function(subdoc, cb) {
      subdoc.$__remove(function(err) {
        cb(err);
      ***REMOVED***);
    ***REMOVED***, function(error) {
      if (error) {
        return _this.schema.s.hooks.execPost('remove:error', _this, [_this], { error: error ***REMOVED***, function(error) {
          next(error);
        ***REMOVED***);
      ***REMOVED***
      next();
    ***REMOVED***);
  ***REMOVED***, null, unshift);
***REMOVED***;
