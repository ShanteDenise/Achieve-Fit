var arrayPush = require('./_arrayPush'),
    isArray = require('./isArray');

/**
 * The base implementation of `getAllKeys` and `getAllKeysIn` which uses
 * `keysFunc` and `symbolsFunc` to get the enumerable property names and
 * symbols of `object`.
 *
 * @private
 * @param {Object***REMOVED*** object The object to query.
 * @param {Function***REMOVED*** keysFunc The function to get the keys of `object`.
 * @param {Function***REMOVED*** symbolsFunc The function to get the symbols of `object`.
 * @returns {Array***REMOVED*** Returns the array of property names and symbols.
 */
function baseGetAllKeys(object, keysFunc, symbolsFunc) {
  var result = keysFunc(object);
  return isArray(object) ? result : arrayPush(result, symbolsFunc(object));
***REMOVED***

module.exports = baseGetAllKeys;
