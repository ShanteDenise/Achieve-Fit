var toInteger = require('./toInteger'),
    toLength = require('./toLength');

/**
 * The base implementation of `_.fill` without an iteratee call guard.
 *
 * @private
 * @param {Array***REMOVED*** array The array to fill.
 * @param {****REMOVED*** value The value to fill `array` with.
 * @param {number***REMOVED*** [start=0] The start position.
 * @param {number***REMOVED*** [end=array.length] The end position.
 * @returns {Array***REMOVED*** Returns `array`.
 */
function baseFill(array, value, start, end) {
  var length = array.length;

  start = toInteger(start);
  if (start < 0) {
    start = -start > length ? 0 : (length + start);
  ***REMOVED***
  end = (end === undefined || end > length) ? length : toInteger(end);
  if (end < 0) {
    end += length;
  ***REMOVED***
  end = start > end ? 0 : toLength(end);
  while (start < end) {
    array[start++] = value;
  ***REMOVED***
  return array;
***REMOVED***

module.exports = baseFill;
