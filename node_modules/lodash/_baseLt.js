/**
 * The base implementation of `_.lt` which doesn't coerce arguments.
 *
 * @private
 * @param {****REMOVED*** value The value to compare.
 * @param {****REMOVED*** other The other value to compare.
 * @returns {boolean***REMOVED*** Returns `true` if `value` is less than `other`,
 *  else `false`.
 */
function baseLt(value, other) {
  return value < other;
***REMOVED***

module.exports = baseLt;
