var baseDifference = require('./_baseDifference'),
    baseFlatten = require('./_baseFlatten'),
    baseRest = require('./_baseRest'),
    isArrayLikeObject = require('./isArrayLikeObject'),
    last = require('./last');

/**
 * This method is like `_.difference` except that it accepts `comparator`
 * which is invoked to compare elements of `array` to `values`. The order and
 * references of result values are determined by the first array. The comparator
 * is invoked with two arguments: (arrVal, othVal).
 *
 * **Note:** Unlike `_.pullAllWith`, this method returns a new array.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Array
 * @param {Array***REMOVED*** array The array to inspect.
 * @param {...Array***REMOVED*** [values] The values to exclude.
 * @param {Function***REMOVED*** [comparator] The comparator invoked per element.
 * @returns {Array***REMOVED*** Returns the new array of filtered values.
 * @example
 *
 * var objects = [{ 'x': 1, 'y': 2 ***REMOVED***, { 'x': 2, 'y': 1 ***REMOVED***];
 *
 * _.differenceWith(objects, [{ 'x': 1, 'y': 2 ***REMOVED***], _.isEqual);
 * // => [{ 'x': 2, 'y': 1 ***REMOVED***]
 */
var differenceWith = baseRest(function(array, values) {
  var comparator = last(values);
  if (isArrayLikeObject(comparator)) {
    comparator = undefined;
  ***REMOVED***
  return isArrayLikeObject(array)
    ? baseDifference(array, baseFlatten(values, 1, isArrayLikeObject, true), undefined, comparator)
    : [];
***REMOVED***);

module.exports = differenceWith;
