/**
 * The base implementation of `_.gt` which doesn't coerce arguments.
 *
 * @private
 * @param {****REMOVED*** value The value to compare.
 * @param {****REMOVED*** other The other value to compare.
 * @returns {boolean***REMOVED*** Returns `true` if `value` is greater than `other`,
 *  else `false`.
 */
function baseGt(value, other) {
  return value > other;
***REMOVED***

module.exports = baseGt;
