var baseIndexOf = require('./_baseIndexOf');

/**
 * Used by `_.trim` and `_.trimEnd` to get the index of the last string symbol
 * that is not found in the character symbols.
 *
 * @private
 * @param {Array***REMOVED*** strSymbols The string symbols to inspect.
 * @param {Array***REMOVED*** chrSymbols The character symbols to find.
 * @returns {number***REMOVED*** Returns the index of the last unmatched string symbol.
 */
function charsEndIndex(strSymbols, chrSymbols) {
  var index = strSymbols.length;

  while (index-- && baseIndexOf(chrSymbols, strSymbols[index], 0) > -1) {***REMOVED***
  return index;
***REMOVED***

module.exports = charsEndIndex;
