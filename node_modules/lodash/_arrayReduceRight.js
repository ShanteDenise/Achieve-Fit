/**
 * A specialized version of `_.reduceRight` for arrays without support for
 * iteratee shorthands.
 *
 * @private
 * @param {Array***REMOVED*** [array] The array to iterate over.
 * @param {Function***REMOVED*** iteratee The function invoked per iteration.
 * @param {****REMOVED*** [accumulator] The initial value.
 * @param {boolean***REMOVED*** [initAccum] Specify using the last element of `array` as
 *  the initial value.
 * @returns {****REMOVED*** Returns the accumulated value.
 */
function arrayReduceRight(array, iteratee, accumulator, initAccum) {
  var length = array == null ? 0 : array.length;
  if (initAccum && length) {
    accumulator = array[--length];
  ***REMOVED***
  while (length--) {
    accumulator = iteratee(accumulator, array[length], length, array);
  ***REMOVED***
  return accumulator;
***REMOVED***

module.exports = arrayReduceRight;
