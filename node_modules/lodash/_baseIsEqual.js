var baseIsEqualDeep = require('./_baseIsEqualDeep'),
    isObjectLike = require('./isObjectLike');

/**
 * The base implementation of `_.isEqual` which supports partial comparisons
 * and tracks traversed objects.
 *
 * @private
 * @param {****REMOVED*** value The value to compare.
 * @param {****REMOVED*** other The other value to compare.
 * @param {boolean***REMOVED*** bitmask The bitmask flags.
 *  1 - Unordered comparison
 *  2 - Partial comparison
 * @param {Function***REMOVED*** [customizer] The function to customize comparisons.
 * @param {Object***REMOVED*** [stack] Tracks traversed `value` and `other` objects.
 * @returns {boolean***REMOVED*** Returns `true` if the values are equivalent, else `false`.
 */
function baseIsEqual(value, other, bitmask, customizer, stack) {
  if (value === other) {
    return true;
  ***REMOVED***
  if (value == null || other == null || (!isObjectLike(value) && !isObjectLike(other))) {
    return value !== value && other !== other;
  ***REMOVED***
  return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
***REMOVED***

module.exports = baseIsEqual;
