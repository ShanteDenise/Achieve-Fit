/**
 * Checks if a stack value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Stack
 * @param {string***REMOVED*** key The key of the entry to check.
 * @returns {boolean***REMOVED*** Returns `true` if an entry for `key` exists, else `false`.
 */
function stackHas(key) {
  return this.__data__.has(key);
***REMOVED***

module.exports = stackHas;
