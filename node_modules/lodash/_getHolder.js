/**
 * Gets the argument placeholder value for `func`.
 *
 * @private
 * @param {Function***REMOVED*** func The function to inspect.
 * @returns {****REMOVED*** Returns the placeholder value.
 */
function getHolder(func) {
  var object = func;
  return object.placeholder;
***REMOVED***

module.exports = getHolder;
