var baseForOwn = require('./_baseForOwn');

/**
 * The base implementation of `_.invert` and `_.invertBy` which inverts
 * `object` with values transformed by `iteratee` and set by `setter`.
 *
 * @private
 * @param {Object***REMOVED*** object The object to iterate over.
 * @param {Function***REMOVED*** setter The function to set `accumulator` values.
 * @param {Function***REMOVED*** iteratee The iteratee to transform values.
 * @param {Object***REMOVED*** accumulator The initial inverted object.
 * @returns {Function***REMOVED*** Returns `accumulator`.
 */
function baseInverter(object, setter, iteratee, accumulator) {
  baseForOwn(object, function(value, key, object) {
    setter(accumulator, iteratee(value), key, object);
  ***REMOVED***);
  return accumulator;
***REMOVED***

module.exports = baseInverter;
