/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeCeil = Math.ceil,
    nativeMax = Math.max;

/**
 * The base implementation of `_.range` and `_.rangeRight` which doesn't
 * coerce arguments.
 *
 * @private
 * @param {number***REMOVED*** start The start of the range.
 * @param {number***REMOVED*** end The end of the range.
 * @param {number***REMOVED*** step The value to increment or decrement by.
 * @param {boolean***REMOVED*** [fromRight] Specify iterating from right to left.
 * @returns {Array***REMOVED*** Returns the range of numbers.
 */
function baseRange(start, end, step, fromRight) {
  var index = -1,
      length = nativeMax(nativeCeil((end - start) / (step || 1)), 0),
      result = Array(length);

  while (length--) {
    result[fromRight ? length : ++index] = start;
    start += step;
  ***REMOVED***
  return result;
***REMOVED***

module.exports = baseRange;
