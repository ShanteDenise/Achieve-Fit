var baseMerge = require('./_baseMerge'),
    isObject = require('./isObject');

/**
 * Used by `_.defaultsDeep` to customize its `_.merge` use to merge source
 * objects into destination objects that are passed thru.
 *
 * @private
 * @param {****REMOVED*** objValue The destination value.
 * @param {****REMOVED*** srcValue The source value.
 * @param {string***REMOVED*** key The key of the property to merge.
 * @param {Object***REMOVED*** object The parent object of `objValue`.
 * @param {Object***REMOVED*** source The parent object of `srcValue`.
 * @param {Object***REMOVED*** [stack] Tracks traversed source values and their merged
 *  counterparts.
 * @returns {****REMOVED*** Returns the value to assign.
 */
function customDefaultsMerge(objValue, srcValue, key, object, source, stack) {
  if (isObject(objValue) && isObject(srcValue)) {
    // Recursively merge objects and arrays (susceptible to call stack limits).
    stack.set(srcValue, objValue);
    baseMerge(objValue, srcValue, undefined, customDefaultsMerge, stack);
    stack['delete'](srcValue);
  ***REMOVED***
  return objValue;
***REMOVED***

module.exports = customDefaultsMerge;
