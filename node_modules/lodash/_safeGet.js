/**
 * Gets the value at `key`, unless `key` is "__proto__".
 *
 * @private
 * @param {Object***REMOVED*** object The object to query.
 * @param {string***REMOVED*** key The key of the property to get.
 * @returns {****REMOVED*** Returns the property value.
 */
function safeGet(object, key) {
  if (key == '__proto__') {
    return;
  ***REMOVED***

  return object[key];
***REMOVED***

module.exports = safeGet;
