/**
 * Checks if `value` is in the array cache.
 *
 * @private
 * @name has
 * @memberOf SetCache
 * @param {****REMOVED*** value The value to search for.
 * @returns {number***REMOVED*** Returns `true` if `value` is found, else `false`.
 */
function setCacheHas(value) {
  return this.__data__.has(value);
***REMOVED***

module.exports = setCacheHas;
