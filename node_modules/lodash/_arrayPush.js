/**
 * Appends the elements of `values` to `array`.
 *
 * @private
 * @param {Array***REMOVED*** array The array to modify.
 * @param {Array***REMOVED*** values The values to append.
 * @returns {Array***REMOVED*** Returns `array`.
 */
function arrayPush(array, values) {
  var index = -1,
      length = values.length,
      offset = array.length;

  while (++index < length) {
    array[offset + index] = values[index];
  ***REMOVED***
  return array;
***REMOVED***

module.exports = arrayPush;
