var baseUniq = require('./_baseUniq');

/**
 * This method is like `_.uniq` except that it accepts `comparator` which
 * is invoked to compare elements of `array`. The order of result values is
 * determined by the order they occur in the array.The comparator is invoked
 * with two arguments: (arrVal, othVal).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Array
 * @param {Array***REMOVED*** array The array to inspect.
 * @param {Function***REMOVED*** [comparator] The comparator invoked per element.
 * @returns {Array***REMOVED*** Returns the new duplicate free array.
 * @example
 *
 * var objects = [{ 'x': 1, 'y': 2 ***REMOVED***, { 'x': 2, 'y': 1 ***REMOVED***, { 'x': 1, 'y': 2 ***REMOVED***];
 *
 * _.uniqWith(objects, _.isEqual);
 * // => [{ 'x': 1, 'y': 2 ***REMOVED***, { 'x': 2, 'y': 1 ***REMOVED***]
 */
function uniqWith(array, comparator) {
  comparator = typeof comparator == 'function' ? comparator : undefined;
  return (array && array.length) ? baseUniq(array, undefined, comparator) : [];
***REMOVED***

module.exports = uniqWith;
