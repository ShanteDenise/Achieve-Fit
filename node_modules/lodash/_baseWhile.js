var baseSlice = require('./_baseSlice');

/**
 * The base implementation of methods like `_.dropWhile` and `_.takeWhile`
 * without support for iteratee shorthands.
 *
 * @private
 * @param {Array***REMOVED*** array The array to query.
 * @param {Function***REMOVED*** predicate The function invoked per iteration.
 * @param {boolean***REMOVED*** [isDrop] Specify dropping elements instead of taking them.
 * @param {boolean***REMOVED*** [fromRight] Specify iterating from right to left.
 * @returns {Array***REMOVED*** Returns the slice of `array`.
 */
function baseWhile(array, predicate, isDrop, fromRight) {
  var length = array.length,
      index = fromRight ? length : -1;

  while ((fromRight ? index-- : ++index < length) &&
    predicate(array[index], index, array)) {***REMOVED***

  return isDrop
    ? baseSlice(array, (fromRight ? 0 : index), (fromRight ? index + 1 : length))
    : baseSlice(array, (fromRight ? index + 1 : 0), (fromRight ? length : index));
***REMOVED***

module.exports = baseWhile;
