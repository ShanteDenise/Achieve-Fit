var baseToNumber = require('./_baseToNumber'),
    baseToString = require('./_baseToString');

/**
 * Creates a function that performs a mathematical operation on two values.
 *
 * @private
 * @param {Function***REMOVED*** operator The function to perform the operation.
 * @param {number***REMOVED*** [defaultValue] The value used for `undefined` arguments.
 * @returns {Function***REMOVED*** Returns the new mathematical operation function.
 */
function createMathOperation(operator, defaultValue) {
  return function(value, other) {
    var result;
    if (value === undefined && other === undefined) {
      return defaultValue;
    ***REMOVED***
    if (value !== undefined) {
      result = value;
    ***REMOVED***
    if (other !== undefined) {
      if (result === undefined) {
        return other;
      ***REMOVED***
      if (typeof value == 'string' || typeof other == 'string') {
        value = baseToString(value);
        other = baseToString(other);
      ***REMOVED***
        value = baseToNumber(value);
        other = baseToNumber(other);
      ***REMOVED***
      result = operator(value, other);
    ***REMOVED***
    return result;
  ***REMOVED***;
***REMOVED***

module.exports = createMathOperation;
