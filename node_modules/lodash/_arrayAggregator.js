/**
 * A specialized version of `baseAggregator` for arrays.
 *
 * @private
 * @param {Array***REMOVED*** [array] The array to iterate over.
 * @param {Function***REMOVED*** setter The function to set `accumulator` values.
 * @param {Function***REMOVED*** iteratee The iteratee to transform keys.
 * @param {Object***REMOVED*** accumulator The initial aggregated object.
 * @returns {Function***REMOVED*** Returns `accumulator`.
 */
function arrayAggregator(array, setter, iteratee, accumulator) {
  var index = -1,
      length = array == null ? 0 : array.length;

  while (++index < length) {
    var value = array[index];
    setter(accumulator, value, iteratee(value), array);
  ***REMOVED***
  return accumulator;
***REMOVED***

module.exports = arrayAggregator;
