var arrayFilter = require('./_arrayFilter'),
    baseRest = require('./_baseRest'),
    baseXor = require('./_baseXor'),
    isArrayLikeObject = require('./isArrayLikeObject'),
    last = require('./last');

/**
 * This method is like `_.xor` except that it accepts `comparator` which is
 * invoked to compare elements of `arrays`. The order of result values is
 * determined by the order they occur in the arrays. The comparator is invoked
 * with two arguments: (arrVal, othVal).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Array
 * @param {...Array***REMOVED*** [arrays] The arrays to inspect.
 * @param {Function***REMOVED*** [comparator] The comparator invoked per element.
 * @returns {Array***REMOVED*** Returns the new array of filtered values.
 * @example
 *
 * var objects = [{ 'x': 1, 'y': 2 ***REMOVED***, { 'x': 2, 'y': 1 ***REMOVED***];
 * var others = [{ 'x': 1, 'y': 1 ***REMOVED***, { 'x': 1, 'y': 2 ***REMOVED***];
 *
 * _.xorWith(objects, others, _.isEqual);
 * // => [{ 'x': 2, 'y': 1 ***REMOVED***, { 'x': 1, 'y': 1 ***REMOVED***]
 */
var xorWith = baseRest(function(arrays) {
  var comparator = last(arrays);
  comparator = typeof comparator == 'function' ? comparator : undefined;
  return baseXor(arrayFilter(arrays, isArrayLikeObject), undefined, comparator);
***REMOVED***);

module.exports = xorWith;
