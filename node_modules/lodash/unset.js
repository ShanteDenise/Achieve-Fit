var baseUnset = require('./_baseUnset');

/**
 * Removes the property at `path` of `object`.
 *
 * **Note:** This method mutates `object`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Object
 * @param {Object***REMOVED*** object The object to modify.
 * @param {Array|string***REMOVED*** path The path of the property to unset.
 * @returns {boolean***REMOVED*** Returns `true` if the property is deleted, else `false`.
 * @example
 *
 * var object = { 'a': [{ 'b': { 'c': 7 ***REMOVED*** ***REMOVED***] ***REMOVED***;
 * _.unset(object, 'a[0].b.c');
 * // => true
 *
 * console.log(object);
 * // => { 'a': [{ 'b': {***REMOVED*** ***REMOVED***] ***REMOVED***;
 *
 * _.unset(object, ['a', '0', 'b', 'c']);
 * // => true
 *
 * console.log(object);
 * // => { 'a': [{ 'b': {***REMOVED*** ***REMOVED***] ***REMOVED***;
 */
function unset(object, path) {
  return object == null ? true : baseUnset(object, path);
***REMOVED***

module.exports = unset;
