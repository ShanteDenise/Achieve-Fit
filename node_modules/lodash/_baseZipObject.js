/**
 * This base implementation of `_.zipObject` which assigns values using `assignFunc`.
 *
 * @private
 * @param {Array***REMOVED*** props The property identifiers.
 * @param {Array***REMOVED*** values The property values.
 * @param {Function***REMOVED*** assignFunc The function to assign values.
 * @returns {Object***REMOVED*** Returns the new object.
 */
function baseZipObject(props, values, assignFunc) {
  var index = -1,
      length = props.length,
      valsLength = values.length,
      result = {***REMOVED***;

  while (++index < length) {
    var value = index < valsLength ? values[index] : undefined;
    assignFunc(result, props[index], value);
  ***REMOVED***
  return result;
***REMOVED***

module.exports = baseZipObject;
