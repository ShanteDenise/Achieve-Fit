/**
 * The base implementation of `_.unary` without support for storing metadata.
 *
 * @private
 * @param {Function***REMOVED*** func The function to cap arguments for.
 * @returns {Function***REMOVED*** Returns the new capped function.
 */
function baseUnary(func) {
  return function(value) {
    return func(value);
  ***REMOVED***;
***REMOVED***

module.exports = baseUnary;
