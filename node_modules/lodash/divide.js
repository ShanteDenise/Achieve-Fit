var createMathOperation = require('./_createMathOperation');

/**
 * Divide two numbers.
 *
 * @static
 * @memberOf _
 * @since 4.7.0
 * @category Math
 * @param {number***REMOVED*** dividend The first number in a division.
 * @param {number***REMOVED*** divisor The second number in a division.
 * @returns {number***REMOVED*** Returns the quotient.
 * @example
 *
 * _.divide(6, 4);
 * // => 1.5
 */
var divide = createMathOperation(function(dividend, divisor) {
  return dividend / divisor;
***REMOVED***, 1);

module.exports = divide;
