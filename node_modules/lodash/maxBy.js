var baseExtremum = require('./_baseExtremum'),
    baseGt = require('./_baseGt'),
    baseIteratee = require('./_baseIteratee');

/**
 * This method is like `_.max` except that it accepts `iteratee` which is
 * invoked for each element in `array` to generate the criterion by which
 * the value is ranked. The iteratee is invoked with one argument: (value).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Math
 * @param {Array***REMOVED*** array The array to iterate over.
 * @param {Function***REMOVED*** [iteratee=_.identity] The iteratee invoked per element.
 * @returns {****REMOVED*** Returns the maximum value.
 * @example
 *
 * var objects = [{ 'n': 1 ***REMOVED***, { 'n': 2 ***REMOVED***];
 *
 * _.maxBy(objects, function(o) { return o.n; ***REMOVED***);
 * // => { 'n': 2 ***REMOVED***
 *
 * // The `_.property` iteratee shorthand.
 * _.maxBy(objects, 'n');
 * // => { 'n': 2 ***REMOVED***
 */
function maxBy(array, iteratee) {
  return (array && array.length)
    ? baseExtremum(array, baseIteratee(iteratee, 2), baseGt)
    : undefined;
***REMOVED***

module.exports = maxBy;
