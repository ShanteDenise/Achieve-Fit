var toArray = require('./toArray');

/**
 * Gets the next value on a wrapped object following the
 * [iterator protocol](https://mdn.io/iteration_protocols#iterator).
 *
 * @name next
 * @memberOf _
 * @since 4.0.0
 * @category Seq
 * @returns {Object***REMOVED*** Returns the next iterator value.
 * @example
 *
 * var wrapped = _([1, 2]);
 *
 * wrapped.next();
 * // => { 'done': false, 'value': 1 ***REMOVED***
 *
 * wrapped.next();
 * // => { 'done': false, 'value': 2 ***REMOVED***
 *
 * wrapped.next();
 * // => { 'done': true, 'value': undefined ***REMOVED***
 */
function wrapperNext() {
  if (this.__values__ === undefined) {
    this.__values__ = toArray(this.value());
  ***REMOVED***
  var done = this.__index__ >= this.__values__.length,
      value = done ? undefined : this.__values__[this.__index__++];

  return { 'done': done, 'value': value ***REMOVED***;
***REMOVED***

module.exports = wrapperNext;
