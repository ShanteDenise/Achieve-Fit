/**
 * The base implementation of `_.findIndex` and `_.findLastIndex` without
 * support for iteratee shorthands.
 *
 * @private
 * @param {Array***REMOVED*** array The array to inspect.
 * @param {Function***REMOVED*** predicate The function invoked per iteration.
 * @param {number***REMOVED*** fromIndex The index to search from.
 * @param {boolean***REMOVED*** [fromRight] Specify iterating from right to left.
 * @returns {number***REMOVED*** Returns the index of the matched value, else `-1`.
 */
function baseFindIndex(array, predicate, fromIndex, fromRight) {
  var length = array.length,
      index = fromIndex + (fromRight ? 1 : -1);

  while ((fromRight ? index-- : ++index < length)) {
    if (predicate(array[index], index, array)) {
      return index;
    ***REMOVED***
  ***REMOVED***
  return -1;
***REMOVED***

module.exports = baseFindIndex;
