var baseInverter = require('./_baseInverter');

/**
 * Creates a function like `_.invertBy`.
 *
 * @private
 * @param {Function***REMOVED*** setter The function to set accumulator values.
 * @param {Function***REMOVED*** toIteratee The function to resolve iteratees.
 * @returns {Function***REMOVED*** Returns the new inverter function.
 */
function createInverter(setter, toIteratee) {
  return function(object, iteratee) {
    return baseInverter(object, setter, toIteratee(iteratee), {***REMOVED***);
  ***REMOVED***;
***REMOVED***

module.exports = createInverter;
