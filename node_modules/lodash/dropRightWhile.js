var baseIteratee = require('./_baseIteratee'),
    baseWhile = require('./_baseWhile');

/**
 * Creates a slice of `array` excluding elements dropped from the end.
 * Elements are dropped until `predicate` returns falsey. The predicate is
 * invoked with three arguments: (value, index, array).
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Array
 * @param {Array***REMOVED*** array The array to query.
 * @param {Function***REMOVED*** [predicate=_.identity] The function invoked per iteration.
 * @returns {Array***REMOVED*** Returns the slice of `array`.
 * @example
 *
 * var users = [
 *   { 'user': 'barney',  'active': true ***REMOVED***,
 *   { 'user': 'fred',    'active': false ***REMOVED***,
 *   { 'user': 'pebbles', 'active': false ***REMOVED***
 * ];
 *
 * _.dropRightWhile(users, function(o) { return !o.active; ***REMOVED***);
 * // => objects for ['barney']
 *
 * // The `_.matches` iteratee shorthand.
 * _.dropRightWhile(users, { 'user': 'pebbles', 'active': false ***REMOVED***);
 * // => objects for ['barney', 'fred']
 *
 * // The `_.matchesProperty` iteratee shorthand.
 * _.dropRightWhile(users, ['active', false]);
 * // => objects for ['barney']
 *
 * // The `_.property` iteratee shorthand.
 * _.dropRightWhile(users, 'active');
 * // => objects for ['barney', 'fred', 'pebbles']
 */
function dropRightWhile(array, predicate) {
  return (array && array.length)
    ? baseWhile(array, baseIteratee(predicate, 3), true, true)
    : [];
***REMOVED***

module.exports = dropRightWhile;
