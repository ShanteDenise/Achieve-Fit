var createFind = require('./_createFind'),
    findLastIndex = require('./findLastIndex');

/**
 * This method is like `_.find` except that it iterates over elements of
 * `collection` from right to left.
 *
 * @static
 * @memberOf _
 * @since 2.0.0
 * @category Collection
 * @param {Array|Object***REMOVED*** collection The collection to inspect.
 * @param {Function***REMOVED*** [predicate=_.identity] The function invoked per iteration.
 * @param {number***REMOVED*** [fromIndex=collection.length-1] The index to search from.
 * @returns {****REMOVED*** Returns the matched element, else `undefined`.
 * @example
 *
 * _.findLast([1, 2, 3, 4], function(n) {
 *   return n % 2 == 1;
 * ***REMOVED***);
 * // => 3
 */
var findLast = createFind(findLastIndex);

module.exports = findLast;
