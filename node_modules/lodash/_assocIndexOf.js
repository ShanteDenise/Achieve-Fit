var eq = require('./eq');

/**
 * Gets the index at which the `key` is found in `array` of key-value pairs.
 *
 * @private
 * @param {Array***REMOVED*** array The array to inspect.
 * @param {****REMOVED*** key The key to search for.
 * @returns {number***REMOVED*** Returns the index of the matched value, else `-1`.
 */
function assocIndexOf(array, key) {
  var length = array.length;
  while (length--) {
    if (eq(array[length][0], key)) {
      return length;
    ***REMOVED***
  ***REMOVED***
  return -1;
***REMOVED***

module.exports = assocIndexOf;
