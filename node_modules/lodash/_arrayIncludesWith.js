/**
 * This function is like `arrayIncludes` except that it accepts a comparator.
 *
 * @private
 * @param {Array***REMOVED*** [array] The array to inspect.
 * @param {****REMOVED*** target The value to search for.
 * @param {Function***REMOVED*** comparator The comparator invoked per element.
 * @returns {boolean***REMOVED*** Returns `true` if `target` is found, else `false`.
 */
function arrayIncludesWith(array, value, comparator) {
  var index = -1,
      length = array == null ? 0 : array.length;

  while (++index < length) {
    if (comparator(value, array[index])) {
      return true;
    ***REMOVED***
  ***REMOVED***
  return false;
***REMOVED***

module.exports = arrayIncludesWith;
