/**
 * Converts `iterator` to an array.
 *
 * @private
 * @param {Object***REMOVED*** iterator The iterator to convert.
 * @returns {Array***REMOVED*** Returns the converted array.
 */
function iteratorToArray(iterator) {
  var data,
      result = [];

  while (!(data = iterator.next()).done) {
    result.push(data.value);
  ***REMOVED***
  return result;
***REMOVED***

module.exports = iteratorToArray;
