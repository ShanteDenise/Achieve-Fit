/**
 * Copies the values of `source` to `array`.
 *
 * @private
 * @param {Array***REMOVED*** source The array to copy values from.
 * @param {Array***REMOVED*** [array=[]] The array to copy values to.
 * @returns {Array***REMOVED*** Returns `array`.
 */
function copyArray(source, array) {
  var index = -1,
      length = source.length;

  array || (array = Array(length));
  while (++index < length) {
    array[index] = source[index];
  ***REMOVED***
  return array;
***REMOVED***

module.exports = copyArray;
