var baseConformsTo = require('./_baseConformsTo'),
    keys = require('./keys');

/**
 * Checks if `object` conforms to `source` by invoking the predicate
 * properties of `source` with the corresponding property values of `object`.
 *
 * **Note:** This method is equivalent to `_.conforms` when `source` is
 * partially applied.
 *
 * @static
 * @memberOf _
 * @since 4.14.0
 * @category Lang
 * @param {Object***REMOVED*** object The object to inspect.
 * @param {Object***REMOVED*** source The object of property predicates to conform to.
 * @returns {boolean***REMOVED*** Returns `true` if `object` conforms, else `false`.
 * @example
 *
 * var object = { 'a': 1, 'b': 2 ***REMOVED***;
 *
 * _.conformsTo(object, { 'b': function(n) { return n > 1; ***REMOVED*** ***REMOVED***);
 * // => true
 *
 * _.conformsTo(object, { 'b': function(n) { return n > 2; ***REMOVED*** ***REMOVED***);
 * // => false
 */
function conformsTo(object, source) {
  return source == null || baseConformsTo(object, source, keys(source));
***REMOVED***

module.exports = conformsTo;
