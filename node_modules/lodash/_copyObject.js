var assignValue = require('./_assignValue'),
    baseAssignValue = require('./_baseAssignValue');

/**
 * Copies properties of `source` to `object`.
 *
 * @private
 * @param {Object***REMOVED*** source The object to copy properties from.
 * @param {Array***REMOVED*** props The property identifiers to copy.
 * @param {Object***REMOVED*** [object={***REMOVED***] The object to copy properties to.
 * @param {Function***REMOVED*** [customizer] The function to customize copied values.
 * @returns {Object***REMOVED*** Returns `object`.
 */
function copyObject(source, props, object, customizer) {
  var isNew = !object;
  object || (object = {***REMOVED***);

  var index = -1,
      length = props.length;

  while (++index < length) {
    var key = props[index];

    var newValue = customizer
      ? customizer(object[key], source[key], key, object, source)
      : undefined;

    if (newValue === undefined) {
      newValue = source[key];
    ***REMOVED***
    if (isNew) {
      baseAssignValue(object, key, newValue);
    ***REMOVED***
      assignValue(object, key, newValue);
    ***REMOVED***
  ***REMOVED***
  return object;
***REMOVED***

module.exports = copyObject;
