/**
 * Converts an ASCII `string` to an array.
 *
 * @private
 * @param {string***REMOVED*** string The string to convert.
 * @returns {Array***REMOVED*** Returns the converted array.
 */
function asciiToArray(string) {
  return string.split('');
***REMOVED***

module.exports = asciiToArray;
