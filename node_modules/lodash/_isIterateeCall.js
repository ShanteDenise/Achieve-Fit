var eq = require('./eq'),
    isArrayLike = require('./isArrayLike'),
    isIndex = require('./_isIndex'),
    isObject = require('./isObject');

/**
 * Checks if the given arguments are from an iteratee call.
 *
 * @private
 * @param {****REMOVED*** value The potential iteratee value argument.
 * @param {****REMOVED*** index The potential iteratee index or key argument.
 * @param {****REMOVED*** object The potential iteratee object argument.
 * @returns {boolean***REMOVED*** Returns `true` if the arguments are from an iteratee call,
 *  else `false`.
 */
function isIterateeCall(value, index, object) {
  if (!isObject(object)) {
    return false;
  ***REMOVED***
  var type = typeof index;
  if (type == 'number'
        ? (isArrayLike(object) && isIndex(index, object.length))
        : (type == 'string' && index in object)
      ) {
    return eq(object[index], value);
  ***REMOVED***
  return false;
***REMOVED***

module.exports = isIterateeCall;
