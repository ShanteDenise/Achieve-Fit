/**
 * This method returns the first argument it receives.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Util
 * @param {****REMOVED*** value Any value.
 * @returns {****REMOVED*** Returns `value`.
 * @example
 *
 * var object = { 'a': 1 ***REMOVED***;
 *
 * console.log(_.identity(object) === object);
 * // => true
 */
function identity(value) {
  return value;
***REMOVED***

module.exports = identity;
