var baseOrderBy = require('./_baseOrderBy'),
    isArray = require('./isArray');

/**
 * This method is like `_.sortBy` except that it allows specifying the sort
 * orders of the iteratees to sort by. If `orders` is unspecified, all values
 * are sorted in ascending order. Otherwise, specify an order of "desc" for
 * descending or "asc" for ascending sort order of corresponding values.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Collection
 * @param {Array|Object***REMOVED*** collection The collection to iterate over.
 * @param {Array[]|Function[]|Object[]|string[]***REMOVED*** [iteratees=[_.identity]]
 *  The iteratees to sort by.
 * @param {string[]***REMOVED*** [orders] The sort orders of `iteratees`.
 * @param- {Object***REMOVED*** [guard] Enables use as an iteratee for methods like `_.reduce`.
 * @returns {Array***REMOVED*** Returns the new sorted array.
 * @example
 *
 * var users = [
 *   { 'user': 'fred',   'age': 48 ***REMOVED***,
 *   { 'user': 'barney', 'age': 34 ***REMOVED***,
 *   { 'user': 'fred',   'age': 40 ***REMOVED***,
 *   { 'user': 'barney', 'age': 36 ***REMOVED***
 * ];
 *
 * // Sort by `user` in ascending order and by `age` in descending order.
 * _.orderBy(users, ['user', 'age'], ['asc', 'desc']);
 * // => objects for [['barney', 36], ['barney', 34], ['fred', 48], ['fred', 40]]
 */
function orderBy(collection, iteratees, orders, guard) {
  if (collection == null) {
    return [];
  ***REMOVED***
  if (!isArray(iteratees)) {
    iteratees = iteratees == null ? [] : [iteratees];
  ***REMOVED***
  orders = guard ? undefined : orders;
  if (!isArray(orders)) {
    orders = orders == null ? [] : [orders];
  ***REMOVED***
  return baseOrderBy(collection, iteratees, orders);
***REMOVED***

module.exports = orderBy;
