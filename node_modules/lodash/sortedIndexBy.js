var baseIteratee = require('./_baseIteratee'),
    baseSortedIndexBy = require('./_baseSortedIndexBy');

/**
 * This method is like `_.sortedIndex` except that it accepts `iteratee`
 * which is invoked for `value` and each element of `array` to compute their
 * sort ranking. The iteratee is invoked with one argument: (value).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Array
 * @param {Array***REMOVED*** array The sorted array to inspect.
 * @param {****REMOVED*** value The value to evaluate.
 * @param {Function***REMOVED*** [iteratee=_.identity] The iteratee invoked per element.
 * @returns {number***REMOVED*** Returns the index at which `value` should be inserted
 *  into `array`.
 * @example
 *
 * var objects = [{ 'x': 4 ***REMOVED***, { 'x': 5 ***REMOVED***];
 *
 * _.sortedIndexBy(objects, { 'x': 4 ***REMOVED***, function(o) { return o.x; ***REMOVED***);
 * // => 0
 *
 * // The `_.property` iteratee shorthand.
 * _.sortedIndexBy(objects, { 'x': 4 ***REMOVED***, 'x');
 * // => 0
 */
function sortedIndexBy(array, value, iteratee) {
  return baseSortedIndexBy(array, value, baseIteratee(iteratee, 2));
***REMOVED***

module.exports = sortedIndexBy;
