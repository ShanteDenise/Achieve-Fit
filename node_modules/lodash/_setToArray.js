/**
 * Converts `set` to an array of its values.
 *
 * @private
 * @param {Object***REMOVED*** set The set to convert.
 * @returns {Array***REMOVED*** Returns the values.
 */
function setToArray(set) {
  var index = -1,
      result = Array(set.size);

  set.forEach(function(value) {
    result[++index] = value;
  ***REMOVED***);
  return result;
***REMOVED***

module.exports = setToArray;
