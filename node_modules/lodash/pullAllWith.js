var basePullAll = require('./_basePullAll');

/**
 * This method is like `_.pullAll` except that it accepts `comparator` which
 * is invoked to compare elements of `array` to `values`. The comparator is
 * invoked with two arguments: (arrVal, othVal).
 *
 * **Note:** Unlike `_.differenceWith`, this method mutates `array`.
 *
 * @static
 * @memberOf _
 * @since 4.6.0
 * @category Array
 * @param {Array***REMOVED*** array The array to modify.
 * @param {Array***REMOVED*** values The values to remove.
 * @param {Function***REMOVED*** [comparator] The comparator invoked per element.
 * @returns {Array***REMOVED*** Returns `array`.
 * @example
 *
 * var array = [{ 'x': 1, 'y': 2 ***REMOVED***, { 'x': 3, 'y': 4 ***REMOVED***, { 'x': 5, 'y': 6 ***REMOVED***];
 *
 * _.pullAllWith(array, [{ 'x': 3, 'y': 4 ***REMOVED***], _.isEqual);
 * console.log(array);
 * // => [{ 'x': 1, 'y': 2 ***REMOVED***, { 'x': 5, 'y': 6 ***REMOVED***]
 */
function pullAllWith(array, values, comparator) {
  return (array && array.length && values && values.length)
    ? basePullAll(array, values, undefined, comparator)
    : array;
***REMOVED***

module.exports = pullAllWith;
