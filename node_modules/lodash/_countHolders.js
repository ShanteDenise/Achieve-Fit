/**
 * Gets the number of `placeholder` occurrences in `array`.
 *
 * @private
 * @param {Array***REMOVED*** array The array to inspect.
 * @param {****REMOVED*** placeholder The placeholder to search for.
 * @returns {number***REMOVED*** Returns the placeholder count.
 */
function countHolders(array, placeholder) {
  var length = array.length,
      result = 0;

  while (length--) {
    if (array[length] === placeholder) {
      ++result;
    ***REMOVED***
  ***REMOVED***
  return result;
***REMOVED***

module.exports = countHolders;
