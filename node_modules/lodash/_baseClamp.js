/**
 * The base implementation of `_.clamp` which doesn't coerce arguments.
 *
 * @private
 * @param {number***REMOVED*** number The number to clamp.
 * @param {number***REMOVED*** [lower] The lower bound.
 * @param {number***REMOVED*** upper The upper bound.
 * @returns {number***REMOVED*** Returns the clamped number.
 */
function baseClamp(number, lower, upper) {
  if (number === number) {
    if (upper !== undefined) {
      number = number <= upper ? number : upper;
    ***REMOVED***
    if (lower !== undefined) {
      number = number >= lower ? number : lower;
    ***REMOVED***
  ***REMOVED***
  return number;
***REMOVED***

module.exports = baseClamp;
