/*!
 * merge-descriptors
 * Copyright(c) 2014 Jonathan Ong
 * Copyright(c) 2015 Douglas Christopher Wilson
 * MIT Licensed
 */

'use strict'

/**
 * Module exports.
 * @public
 */

module.exports = merge

/**
 * Module variables.
 * @private
 */

var hasOwnProperty = Object.prototype.hasOwnProperty

/**
 * Merge the property descriptors of `src` into `dest`
 *
 * @param {object***REMOVED*** dest Object to add descriptors to
 * @param {object***REMOVED*** src Object to clone descriptors from
 * @param {boolean***REMOVED*** [redefine=true] Redefine `dest` properties with `src` properties
 * @returns {object***REMOVED*** Reference to dest
 * @public
 */

function merge(dest, src, redefine) {
  if (!dest) {
    throw new TypeError('argument dest is required')
  ***REMOVED***

  if (!src) {
    throw new TypeError('argument src is required')
  ***REMOVED***

  if (redefine === undefined) {
    // Default to true
    redefine = true
  ***REMOVED***

  Object.getOwnPropertyNames(src).forEach(function forEachOwnPropertyName(name) {
    if (!redefine && hasOwnProperty.call(dest, name)) {
      // Skip desriptor
      return
    ***REMOVED***

    // Copy descriptor
    var descriptor = Object.getOwnPropertyDescriptor(src, name)
    Object.defineProperty(dest, name, descriptor)
  ***REMOVED***)

  return dest
***REMOVED***
